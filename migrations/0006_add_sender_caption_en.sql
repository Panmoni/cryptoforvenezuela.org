-- Machine-translated (Workers AI, at approve time) English version of
-- sender_caption. Kept alongside the original Spanish rather than replacing
-- it — the site shows both, never translation-only.
ALTER TABLE media ADD COLUMN sender_caption_en TEXT;
